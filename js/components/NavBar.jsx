let React = require('react');

let NavBar = React.createClass({
  render: function() {
    return (
      <div className="navBar">
        Hello, world! I am a NavBar.
      </div>
    );
  }
});

module.exports = NavBar;
